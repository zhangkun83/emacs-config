#!/bin/bash
# List all running emacs servers
serverdir="${TMPDIR:-/tmp}/emacs${UID}"
for file in ${serverdir}/*; do
  if [[ -S ${file} ]]; then
    printf '%s\t%s\n' "${file##*/}" $(fuser "${file}" 2> /dev/null)
  fi
done

