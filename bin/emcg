#!/bin/bash
# Runs an emacs client in a new GUI window
if [ "$#" -lt 1 ]; then
  echo "Usage: emcg <server_name> [args...]"
  exit 1
fi
server_name="$1"
shift
emacsclient -c -s "$HOME/.emacs.d/sockets/$server_name" "$@"
