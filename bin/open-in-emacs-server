#!/bin/bash
# Open a file in a running emacs server.
if [ $# -lt 2 ]; then
    echo "Usage: open-in-emacs-server <server_name> <file>"
    exit 1
fi

SERVER_NAME="$1"
FILE="$2"

emacsclient -s "$SERVER_NAME" -e "(find-file \"$FILE\")"

read -p "Press enter when you have done editing ..."
